"""
Previously generated a synthetic SQLite file for development.

The IS 455 assignment expects the instructor `shop.db` at the assignment
project root. The app and Python jobs read that file via `jobs/config.py`
(`OP_DB_PATH`).

Do not run this script; it is kept only so old instructions that mention
`seed_shop_db.py` fail loudly instead of overwriting your database.
"""

raise SystemExit(
    "seed_shop_db.py is disabled. Use shop.db at the assignment root (see jobs/config.py)."
)
